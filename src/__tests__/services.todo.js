test("getStations returns stations", () => {
  // const stations = [{ iata: "ABZ", latitude: 57.2019, longitude: -2.1977, shortName: "Aberdeen", connections: [] }];
  //
  // Import axios and getStations
  // Mock the module
  // Resolve the `get` method with a custom response
  // Check that the response is equal to the previously set response
  // Check that axios was walled only once and with the right parameters
  // Restore the mock
});
