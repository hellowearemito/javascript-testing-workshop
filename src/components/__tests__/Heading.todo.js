// import { mount } from "@vue/test-utils";
// import Heading from "../Heading.vue";

describe("Heading", () => {
  test("has the expected HTML structure", () => {
    // Check if the HTML of the component is matching the saved snapshot.
    // See toMatchSnapshot: https://jestjs.io/docs/en/expect#tomatchsnapshotpropertymatchers-hint
    // Hint: You can access the native HTML element via `wrapper.element`
    // const wrapper = mount(Heading);
  });
});
