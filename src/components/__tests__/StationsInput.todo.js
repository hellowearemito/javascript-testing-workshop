// import { mount } from "@vue/test-utils";
// import StationsInput from "../StationsInput.vue";

// let stations;
// beforeEach(() => {
//   stations = require("../../../stations.json");
// });

describe("StationsInput", () => {
  it("displays the list of stations when the input is focused", () => {
    // const wrapper = mount(StationsInput, {
    //   propsData: {
    //     storeProp: "origin",
    //     stations,
    //     direction: "outbound"
    //   }
    // });
    //
    // Find the `input` within `wrapper.element`
    // Create a new event for "focus": https://developer.mozilla.org/en-US/docs/Web/API/Event/Event
    // Dispatch this event on the input
    // Assert that the `.station-list` element is visible
  });

  it("displays only the stations matching the input", () => {
    // const wrapper = mount(StationsInput, {
    //   propsData: {
    //     storeProp: "origin",
    //     stations,
    //     direction: "outbound"
    //   }
    // });
    //
    // Find the `input` within `wrapper.element`
    // Create a new event for "focus": https://developer.mozilla.org/en-US/docs/Web/API/Event/Event
    // Dispatch this event on the input
    // Set the input's value to a string that matches some of stations (e.g. "ale")
    // Hint: don't forget to dispatch an 'input' event
    // Assert that the elements in the `.station-list` are the filtered stations
    // In case of "ale" they are: Alesund (AES) and Valencia (VLC)
  });
});
